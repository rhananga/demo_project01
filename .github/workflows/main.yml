name: Install npm

on:
  push:
    branches: 
    - main
jobs:

  build:
    runs-on: b11cfab0941c
    
    steps:
    - uses: actions/checkout@v3
    - name: Use Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18.x'
        
    - name: Install npm
      run: npm install
      
    - name: Build project
      run: npm run build
